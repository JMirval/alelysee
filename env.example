## Server (Dioxus fullstack)
#
# Required to run `dx serve` with server functions + Postgres.

# Postgres (RDS or local)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/alelysee

# AWS Cognito (Hosted UI)
COGNITO_REGION=us-east-1
COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
COGNITO_APP_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX
COGNITO_DOMAIN=https://your-domain.auth.us-east-1.amazoncognito.com

# Must match the callback URL configured in the Cognito App Client
COGNITO_REDIRECT_URI=http://localhost:8080/auth/callback

# S3 uploads
S3_BUCKET=your-s3-bucket-name

# Optional: for video playback (recommended)
# Example: https://d111111abcdef8.cloudfront.net
CLOUDFRONT_BASE_URL=

# AWS credentials/region (standard AWS SDK environment variables)
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=


