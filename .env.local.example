## Local Development Mode
#
# Zero external dependencies - perfect for quick iteration.
# Copy to .env: `cp .env.local.example .env`

APP_MODE=local

# JWT secret for local email/password auth (min 32 chars)
# This example value is fine for local development
JWT_SECRET=local-dev-secret-key-min-32-characters-long

# App base URL for email links (verification, password reset)
APP_BASE_URL=http://localhost:8080

# All other services (database, email, storage) are handled automatically:
# - Database: SQLite at .dev/local.db (auto-created, auto-seeded)
# - Email: Logged to console (check terminal for verification codes)
# - Storage: Filesystem at .dev/uploads/ (auto-created)
DATABASE_URL=.dev/local.db